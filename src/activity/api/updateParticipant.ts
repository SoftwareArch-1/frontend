import { updateParticipantDto } from '../../core/sync-with-backend/dto/activity/dto/updateParticipantDto'

export const updateParticipant = async (dto: updateParticipantDto) => {
  //   const { data } = await axiosInstance.get(apiPath.activity.getActivities())
  //   return activityListDtoSchema.parse(data)

  //mock database
  return true
}
